{{- if (index .Values "postgresql-sts").enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "erpnext.fullname" . }}-postgresql-sts
  labels:
    {{- include "erpnext.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "erpnext.name" . }}-postgresql-sts
      app.kubernetes.io/instance: {{ .Release.Name }}
  serviceName: {{ include "erpnext.fullname" . }}-postgresql-sts
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "erpnext.name" . }}-postgresql-sts
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: postgresql
          image: "{{ (index .Values "postgresql-sts").image.repository }}:{{ (index .Values "postgresql-sts").image.tag }}"
          imagePullPolicy: {{ (index .Values "postgresql-sts").image.pullPolicy }}
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "erpnext.fullname" . }}
                  key: postgres-password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - name: postgresql
              containerPort: 5432
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml (index .Values "postgresql-sts").resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        storageClassName: {{ (index .Values "postgresql-sts").persistence.storageClass }}
        resources:
          requests:
            storage: {{ (index .Values "postgresql-sts").persistence.size }}
{{- end }}
