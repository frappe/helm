{{- if (index .Values "postgresql-sts").enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "erpnext.fullname" . }}-postgresql-sts
  labels:
    {{- include "erpnext.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgresql
      protocol: TCP
      name: postgresql
  selector:
    app.kubernetes.io/name: {{ include "erpnext.name" . }}-postgresql-sts
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
