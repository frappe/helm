{{- if .Values.jobs.custom.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Values.jobs.custom.jobName }}
  labels:
    {{- if .Values.jobs.custom.labels }}
    {{ toYaml .Values.jobs.custom.labels | indent 4 }}
    {{- else}}
    {{- include "erpnext.labels" . | nindent 4 }}
    {{- end }}
spec:
  backoffLimit: {{ .Values.jobs.custom.backoffLimit }}
  template:
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      serviceAccountName: {{ template "erpnext.serviceAccountName" $ }}
      initContainers: {{ .Values.jobs.custom.initContainers }}
      securityContext:
        {{- toYaml $.Values.podSecurityContext | nindent 8 }}
      containers: {{ .Values.jobs.custom.containers }}
      restartPolicy: {{ .Values.jobs.custom.restartPolicy }}
      volumes: {{ .Values.jobs.custom.volumes }}
      {{- with .Values.jobs.custom.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.jobs.custom.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.jobs.custom.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
